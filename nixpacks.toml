[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.install]
cmds = ["pip install setuptools wheel", "pip install --no-build-isolation -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
